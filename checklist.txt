No deberia hacer falta: docker run -p 8080:8080 -e AI_API_KEY=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 ai-coding-tutor-ide-plugin-backend

#DOCKER
docker ps (-a)
docker-compose up (-d --build)
docker exec -it ai-coding-tutor-ide-plugin-backend-1 /bin/bash    (or bin/sh)
docker exec -it ai-coding-tutor-ide-plugin-backend-1 env  (to see env variables, "env" inside the container also works)
docker logs container_name  (to check docker logs and ensure api key is setup)
curl -X POST -H "Content-Type: application/json" -d '{"query":"How to write a loop? Keep the answer short","level":"novice"}' http://localhost:8080/query  (to test the backend)


#Checking database
docker exec -it ai-coding-tutor-ide-plugin-db-1 psql -U user -d mydb
\dt
SELECT COUNT(*) FROM queries;

#feedback
curl -X POST -H "Content-Type: application/json" -d '{"id":"returned-id","feedback":"positive"}' http://localhost:8080/feedback

#SWAGGER
Ejecutar desde el ./backend
swag init -g cmd/server/main.go
Once the server is running:
http://localhost:8080/swagger/index.html




TODO LIST
-La extension no deberia de actualizarse automaticamente con el paso del tiempo.
-"Get suggestion" puede ser alterado, actualmente cuando hacer hover, te da el consejo, pero si le das click devuelve un error.
-Por ver: CUando se le de click, podria generarte una sugerencia mas detallada y que se vea de forma mas visual
-La pantalla a la izquierda merece cambios.
-Idea: Se podria eliminar el texto "Ger suggestion" all together para minimizar la intrusion de la ia, y cuando le das click, que aparte del mensaje que muestra sobre las posibles mejoras, aparezca a al derecha un boton para que te haga las correcciones automaticamente.
-Mejorar el promt, especificamente el de analyze.
-Elegir un proveedor de cloud 
-Implementar una documentacion (swagger quizas tmb for the backend)
-Add logs for frontend or backend(perhaps add a .dockerignore if added logs on back)
-Añadir opcion de lenguage de la ia a la derecha, solo habria que cambiar el promt a español de momento.
